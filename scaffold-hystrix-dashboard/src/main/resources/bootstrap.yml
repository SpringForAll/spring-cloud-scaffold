server:
  port: 8050
spring:
  application:
    name: scaffold-hystrix-dashboard
eureka:
 instance:
   hostname: localhost
 client:
   registerWithEureka: true
   fetchRegistry: true
   serviceUrl:
           defaultZone: http://localhost:8761/eureka/
